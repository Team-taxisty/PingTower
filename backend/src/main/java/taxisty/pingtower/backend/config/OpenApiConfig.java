package taxisty.pingtower.backend.config;

import org.springframework.context.annotation.Configuration;

/**
 * OpenAPI 3.0 configuration for PingTower API documentation.
 * Most configuration is handled in application.yaml, this class can be extended
 * for more complex programmatic configuration if needed.
 */
@Configuration
public class OpenApiConfig {
    // Configuration is primarily handled via application.yaml
    // This class is reserved for future programmatic OpenAPI configuration
}